#include <stdio.h>
#include <stdlib.h>
#include "orderedList.h"


void main(int argc, char constant *argv[]){
    insert (element);
    remove_b_index(index);
    removeByElement(element);
    search(element);
    index_of(element);
    get(index);
    count(element);
    size();
    merge(L1,L2);
    is_empty();
    clear();
}